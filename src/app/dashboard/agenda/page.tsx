/* eslint-disable react-hooks/purity */
import { CalendarDays, Clock3, Plus, UsersRound } from "lucide-react";
import { requireOrganization } from "@/lib/auth/session";
import { createOperationalClient } from "@/lib/supabase/operational";
import { createClassAction } from "../operational-actions";
import { buttonClass, inputClass, PageTitle } from "../ui";

export default async function SchedulePage(){const c=await requireOrganization();const o=c.activeOrganization as unknown as{id:string};const db=await createOperationalClient();const [{data:branches},{data:sessions}]=await Promise.all([db.from("branches").select("id,name").eq("organization_id",o.id),db.from("class_sessions").select("id,title,starts_at,ends_at,capacity,status,branch:branches(name),bookings:class_bookings(count)").eq("organization_id",o.id).order("starts_at").gte("starts_at",new Date(Date.now()-86400000).toISOString()).limit(60)]);return <><PageTitle eyebrow="GRADE E OCUPAÇÃO" title="Agenda de aulas" description="Turmas, horários, capacidade e presença por unidade." badge={`${sessions?.length??0} AULAS`}/><section className="dashboard-panel mt-8 p-5"><h2 className="flex items-center gap-2 font-semibold"><Plus size={17} className="text-emerald-300"/>Nova aula</h2><form action={createClassAction} className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-5"><input className={inputClass} required name="title" placeholder="Nome da aula"/><select className={inputClass} required name="branch_id"><option value="">Selecione a unidade</option>{branches?.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select><input className={inputClass} required type="datetime-local" name="starts_at"/><input className={inputClass} type="number" name="capacity" placeholder="Capacidade"/><button className={buttonClass}>Agendar</button></form></section><section className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{sessions?.map(session=>{const branch=session.branch as unknown as{name?:string};const bookings=session.bookings as unknown as Array<{count:number}>;return <article className="dashboard-panel p-5" key={session.id}><div className="flex items-center justify-between"><CalendarDays size={19} className="text-emerald-300"/><span className="text-[9px] font-bold text-emerald-300">{session.status.toUpperCase()}</span></div><h2 className="mt-5 text-lg font-semibold">{session.title}</h2><p className="mt-2 flex items-center gap-2 text-xs text-emerald-50/45"><Clock3 size={13}/>{new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(session.starts_at))}</p><div className="mt-4 flex items-center justify-between border-t border-emerald-100/10 pt-4 text-[10px] text-emerald-50/35"><span>{branch?.name}</span><span className="flex items-center gap-1"><UsersRound size={12}/>{bookings?.[0]?.count??0}/{session.capacity}</span></div></article>})}{!sessions?.length&&<div className="dashboard-panel grid min-h-48 place-items-center text-sm text-emerald-50/30">Crie a primeira aula da grade.</div>}</section></>}
