/* eslint-disable react-hooks/purity */
import { CalendarDays, Clock3, Plus, UserPlus, UsersRound } from "lucide-react";
import { requireOrganization } from "@/lib/auth/session";
import { createOperationalClient } from "@/lib/supabase/operational";
import { bookClassAction, createClassAction } from "../operational-actions";
import { buttonClass, inputClass, PageTitle } from "../ui";

export default async function SchedulePage(){const c=await requireOrganization();const o=c.activeOrganization as unknown as{id:string};const db=await createOperationalClient();const cutoff=new Date(Date.now()-86400000);const[{data:branches},{data:sessions},{data:members}]=await Promise.all([db.from("branches").select("id,name").eq("organization_id",o.id),db.from("class_sessions").select("id,title,starts_at,ends_at,capacity,status,branch:branches(name),bookings:class_bookings(id,status,member:members(full_name))").eq("organization_id",o.id).order("starts_at").gte("starts_at",cutoff.toISOString()).limit(60),db.from("members").select("id,full_name").eq("organization_id",o.id).eq("status","active").order("full_name")]);return <><PageTitle eyebrow="GRADE E OCUPAÇÃO" title="Agenda de aulas" description="Turmas, reservas, capacidade e presença por unidade." badge={`${sessions?.length??0} AULAS`}/><div className="mt-8 grid gap-5 xl:grid-cols-2"><section className="dashboard-panel p-5"><h2 className="flex items-center gap-2 font-semibold"><Plus size={17} className="text-emerald-300"/>Nova aula</h2><form action={createClassAction} className="mt-4 grid gap-3"><input className={inputClass} required name="title" placeholder="Nome da aula"/><select className={inputClass} required name="branch_id"><option value="">Selecione a unidade</option>{branches?.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select><div className="grid grid-cols-2 gap-3"><input className={inputClass} required type="datetime-local" name="starts_at"/><input className={inputClass} type="number" name="capacity" placeholder="Capacidade"/></div><button className={buttonClass}>Agendar aula</button></form></section><section className="dashboard-panel p-5"><h2 className="flex items-center gap-2 font-semibold"><UserPlus size={17} className="text-cyan-300"/>Nova reserva</h2><form action={bookClassAction} className="mt-4 grid gap-3"><select className={inputClass} required name="session_id"><option value="">Selecione a aula</option>{sessions?.filter(s=>s.status==="scheduled").map(s=><option key={s.id} value={s.id}>{s.title} · {new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(s.starts_at))}</option>)}</select><select className={inputClass} required name="member_id"><option value="">Selecione o aluno</option>{members?.map(m=><option key={m.id} value={m.id}>{m.full_name}</option>)}</select><button className={buttonClass}>Confirmar reserva</button></form></section></div><section className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{sessions?.map(session=>{const branch=session.branch as unknown as{name?:string};const bookings=session.bookings as unknown as Array<{id:string;status:string;member?:{full_name?:string}}> ;return <article className="dashboard-panel p-5" key={session.id}><div className="flex items-center justify-between"><CalendarDays size={19} className="text-emerald-300"/><span className="text-[9px] font-bold text-emerald-300">{session.status.toUpperCase()}</span></div><h2 className="mt-5 text-lg font-semibold">{session.title}</h2><p className="mt-2 flex items-center gap-2 text-xs text-emerald-50/45"><Clock3 size={13}/>{new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(session.starts_at))}</p><div className="mt-4 flex items-center justify-between border-t border-emerald-100/10 pt-4 text-[10px] text-emerald-50/35"><span>{branch?.name}</span><span className="flex items-center gap-1"><UsersRound size={12}/>{bookings?.length??0}/{session.capacity}</span></div>{bookings?.length>0&&<div className="mt-3 flex flex-wrap gap-1">{bookings.slice(0,5).map(b=><span className="rounded-full bg-emerald-300/10 px-2 py-1 text-[8px] text-emerald-300" key={b.id}>{b.member?.full_name}</span>)}</div>}</article>})}</section></>}
