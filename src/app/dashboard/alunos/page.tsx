import { Plus, Search, UserRound } from "lucide-react";
import { requireOrganization } from "@/lib/auth/session";
import { createOperationalClient, shortDate } from "@/lib/supabase/operational";
import { createMemberAction } from "../operational-actions";
import { buttonClass, inputClass, PageTitle } from "../ui";

export default async function MembersPage() { const context=await requireOrganization(); const organization=context.activeOrganization as unknown as {id:string}; const db=await createOperationalClient(); const [{data:members},{data:branches}]=await Promise.all([db.from("members").select("id,full_name,email,phone,status,goal,joined_at,branch:branches(name)").eq("organization_id",organization.id).order("created_at",{ascending:false}),db.from("branches").select("id,name").eq("organization_id",organization.id)]); return <><PageTitle eyebrow="BASE DE RELACIONAMENTO" title="Alunos" description="Cadastro completo, situação do vínculo e contexto para atendimento e retenção." badge={`${members?.length??0} ALUNOS`} /><section className="dashboard-panel mt-8 p-5"><h2 className="flex items-center gap-2 font-semibold"><Plus size={17} className="text-emerald-300"/>Novo aluno</h2><form action={createMemberAction} className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-4"><input className={inputClass} name="full_name" required placeholder="Nome completo"/><input className={inputClass} name="email" type="email" placeholder="E-mail"/><input className={inputClass} name="phone" placeholder="WhatsApp"/><input className={inputClass} name="cpf" placeholder="CPF"/><input className={inputClass} name="goal" placeholder="Objetivo principal"/><select className={inputClass} name="branch_id"><option value="">Sem unidade</option>{branches?.map(branch=><option key={branch.id} value={branch.id}>{branch.name}</option>)}</select><button className={buttonClass}>Cadastrar aluno</button></form></section><section className="dashboard-panel mt-6 overflow-hidden"><div className="flex items-center justify-between border-b border-emerald-100/10 p-5"><div><h2 className="font-semibold">Base de alunos</h2><p className="mt-1 text-xs text-emerald-50/35">Dados reais da organização</p></div><Search size={17} className="text-emerald-300"/></div>{members?.length?members.map(member=>{const branch=member.branch as unknown as {name?:string}|null; return <article className="grid gap-3 border-b border-emerald-100/[.06] p-5 last:border-0 sm:grid-cols-[1.3fr_1fr_1fr_auto] sm:items-center" key={member.id}><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-full bg-emerald-300/10 text-emerald-300"><UserRound size={16}/></span><div><strong className="block text-sm">{member.full_name}</strong><span className="text-[10px] text-emerald-50/35">{member.goal||"Objetivo não informado"}</span></div></div><span className="text-xs text-emerald-50/45">{branch?.name||"Sem unidade"}</span><span className="text-xs text-emerald-50/45">Desde {shortDate(member.joined_at)}</span><span className="w-fit rounded-full bg-emerald-300/10 px-2 py-1 text-[9px] font-bold text-emerald-300">{member.status.toUpperCase()}</span></article>}):<div className="grid min-h-48 place-items-center text-sm text-emerald-50/30">Cadastre o primeiro aluno.</div>}</section></> }
